function square(x: number): number {
  return x * x;
}

// Example usage
let mySquare = square(5);
console.log(mySquare); // Outputs 25

function addNumbers(a: number, b: number): number {
  return a + b;
}

addNumbers(3, 4); // Outputs 7
